query GetListings($filters: FiltersInput) {
  listings(filters: $filters) {
    _id
    type
    price
    title
    description
    rating
    availabilityRange
    location {
      address
      city
      country
    }
  }
}

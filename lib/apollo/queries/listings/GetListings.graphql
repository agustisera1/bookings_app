query GetListings($filters: FiltersInput) {
  listings(filters: $filters) {
    _id
    type
    price
    title
    description
    photos
    rating
    availabilityRange
    location {
      address
      city
      country
    }
  }
}

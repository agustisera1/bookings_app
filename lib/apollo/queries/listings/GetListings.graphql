query GetListings($limit: Int = 30, $term: String, $own: Boolean) {
  listings(limit: $limit, term: $term, own: $own) {
    _id
    type
    price
    title
    description
    location {
      address
      city
      country
    }
  }
}
